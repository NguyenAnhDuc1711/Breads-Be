# Feature flags — Breads-Be

# Lược field optional rỗng (survey/files/...) khỏi response Post (epic lean-api-response,
# FR-5). Mặc định OFF (an toàn) — chỉ đọc "true" là ON, mọi giá trị khác (bao gồm absent,
# "false", chuỗi rác) đều fail-safe về OFF. Rollback runbook: xem comment đầu file
# `src/api/services/config.ts`.
POST_RESPONSE_FIELD_FILTER_ENABLED=false
